#include "main.h"

/**
 * _isdigit - checks for numbers 0 to 9
 * @c: character to checked
 *Return: 0 or 1 depending on the output
 */

int _isdigit(int c)
{
	if ((c >= 48) && (c <= 57))
	{
		return (1);
	}
}
